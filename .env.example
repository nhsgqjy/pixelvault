# Copy to .env and replace this value before starting production.
PIXELVAULT_PASSWORD=replace-with-a-long-random-password
PIXELVAULT_PORT=8080

# Keep false for direct HTTP on a trusted LAN. Set true only behind HTTPS.
PIXELVAULT_COOKIE_SECURE=false
